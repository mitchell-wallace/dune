{
  "name": "Claude Code Sandbox",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "TZ": "${localEnv:TZ:Australia/Melbourne}",
      "CLAUDE_CODE_VERSION": "latest",
      "GIT_DELTA_VERSION": "0.18.2",
      "ZSH_IN_DOCKER_VERSION": "1.2.0",
      "DUNE_BUILD_MODE": "${localEnv:DUNE_BUILD_MODE:std}",
      "DUNE_BUILD_GEAR": "${localEnv:DUNE_BUILD_GEAR:}",
      "DUNE_PYTHON_VERSION": "${localEnv:DUNE_PYTHON_VERSION:}",
      "DUNE_UV_VERSION": "${localEnv:DUNE_UV_VERSION:}",
      "DUNE_GO_VERSION": "${localEnv:DUNE_GO_VERSION:}",
      "DUNE_RUST_VERSION": "${localEnv:DUNE_RUST_VERSION:}"
    }
  },
  "runArgs": [
    "--cap-add=NET_ADMIN",
    "--cap-add=NET_RAW"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "biomejs.biome"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "biomejs.biome",
        "editor.codeActionsOnSave": {
          "source.fixAll.eslint": "explicit"
        },
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "bash",
            "icon": "terminal-bash"
          },
          "zsh": {
            "path": "zsh"
          }
        }
      }
    }
  },
  "remoteUser": "node",
  "mounts": [
    "source=claude-code-bashhistory-${devcontainerId},target=/commandhistory,type=volume",
    "source=agent-persist-${localEnv:DUNE_PROFILE:0},target=/persist/agent,type=volume"
  ],
  "containerEnv": {
    "NODE_OPTIONS": "--max-old-space-size=4096",
    "CLAUDE_CONFIG_DIR": "/home/node/.claude",
    "POWERLEVEL9K_DISABLE_GITSTATUS": "true",
    "TZ": "${localEnv:TZ:Australia/Melbourne}",
    "LANG": "${localEnv:LANG:en_AU.UTF-8}",
    "LANGUAGE": "${localEnv:LANGUAGE:en_AU:en}",
    "DUNE_PROFILE": "${localEnv:DUNE_PROFILE:0}",
    "DUNE_SECURITY_MODE": "${localEnv:DUNE_SECURITY_MODE:std}",
    "DUNE_WORKSPACE_MODE": "${localEnv:DUNE_WORKSPACE_MODE:mount}"
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=delegated",
  "workspaceFolder": "/workspace",
  "postStartCommand": "/usr/local/bin/dune-poststart.sh",
  "waitFor": "postStartCommand"
}
